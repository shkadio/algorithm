#include <stdio.h>

int main()
{
// 1-4：鸡兔同笼：已知鸡和兔的总数量n及它们腿的总数量m，求鸡和兔各多少只
    // int r,c,n,m;
    // scanf("%d%d",&n,&m);
    // r = (m - 2*n)/2;
    // c = n - r;
    // if (m%2==1 || r <0 || c < 0)
    //     printf("No answer\n");
    // else
    //     printf("%d %d\n",c,r);

//提示1-17：c语言中的逻辑运算符都是短路运算符，一旦确定整个表达式的值，就不再继续计算

//1-5：三整数排序：输出3个整数，从小到大排序后输出
    // int a,b,c,t;
    // scanf("%d%d%d",&a,&b,&c);
    // if(a > b) {t = a; a = b; b = t;} // a <= b
    // if(a > c) {t = a; a = c; c = t;} // a <= c
    // if(b > c) {t = b; b = c; c = t;} // c <= b
    // printf("%d %d %d\n",a,b,c);           
    return 0;
}